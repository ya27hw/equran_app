# Defect register

Only evidence-backed findings are listed as verified defects. Hypotheses remain risks until a test or profile confirms them.

| ID | Classification | Severity | Evidence / reproduction | Affected files | Resolution / coverage | Commit |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | Data-loss risk | Critical | `BackupService.restoreFromPickedFile()` validated payload, then cleared live boxes before writing. | `lib/backend/backup_service.dart` | Fixed in core: section validation precedes writes; all affected boxes are snapshotted and restored on failure. Deterministic validation tests pass. | Current checkpoint |
| D-002 | Correctness / financial integrity | High | `_fetchLiveMetalPrices()` requested PAXG and calculated silver as `goldGramPrice / 80`, then reported synchronized live rates. | `lib/zakat/zakat_page.dart` | Fixed in core: independent provider fields, source/time/unit/currency, stale/manual/offline states, and provider/snapshot tests. | Current checkpoint |
| D-003 | Maintainability / quality gate | High | `flutter test` exited 1 because no `test/` directory existed; CI quality only ran `flutter pub get`. | `.github/workflows/deploy.yml`, `test/` | Fixed in core: deterministic suite, fatal analysis, format, localization/dependency checks, coverage, and Android debug build gates. | Current checkpoint |
| D-004 | Performance defect | High | `QuranTextSearchService._buildIndex()` looped over every ayah and selected translation; `Future.microtask` left CPU work on the UI isolate. | `lib/search/quran_text_search_service.dart` | Core mitigation: no retained full index and bounded asynchronous batches. A dedicated worker/index benchmark remains open. | Current checkpoint / follow-up |
| D-005 | Performance / memory defect | High | `QpcV4FontService.ensureFontLoadedForPage()` loaded and registered both light and dark variants for every page. | `lib/backend/qpc_v4_font_service.dart` | Core mitigation: active-theme-only loading, shared futures, bounded preload, prompt byte release, and theme cache invalidation. Engine memory profiling remains open. | Current checkpoint / follow-up |
| D-006 | Performance / reliability | High | `AudioDownloadService` called `readAsBytes()` and `decodeBytes()` for full archives before extraction. | `lib/backend/audio_downloads.dart` | Core mitigation: disk input stream, file/count/size/symlink bounds, free-memory writes, and cleanup. Worker/cancel/resume fixtures remain open. | Current checkpoint / follow-up |
| D-007 | Startup performance | High | `main()` awaited storage, migration, translation, prayer, and widget work before `runApp()`. | `lib/main.dart` | Fixed in core: blocking settings bootstrap plus idempotent post-frame coordinator; startup trace/device measurement remains open. | Current checkpoint / follow-up |
| D-008 | Product/security configuration | Medium | Release signing fell back to debug signing when credentials were absent. | `android/app/build.gradle` | Fixed in core: CI/`-PrequireReleaseSigning` fails closed; local debug-signed release remains explicitly nonproduction. | Current checkpoint |
| D-009 | Accessibility/localization risk | Medium | New roadmap UI did not yet exist and repository had no automated RTL, text-scaling, semantics, or ARB drift checks. | `test/`, `lib/l10n/`, CI | Core mitigation: virtualized accessible Memory Map foundation, localized ARB parity, generated-code verification, and semantics/controller tests. Full UI integration checks remain open. | Current checkpoint / follow-up |
| D-010 | Privacy risk | Medium | No centralized local feature-flag/data-retention policy existed for roadmap systems. | `lib/`, `docs/roadmap/` | Core mitigation: local fail-closed flags, local stores, independent Memory Twin reset, voice-path backup exclusion, and privacy record. Transport encryption/complete deletion UI remain open. | Current checkpoint / follow-up |
