import 'package:flutter/material.dart';

class TestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: const Text(
          "This is the about page. The page here goes into detail the copyright issues etc..."),
    );
  }
}
